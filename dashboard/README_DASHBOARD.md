# Dashboard

The submitted dashboard export is included as `bluestock_mf_dashboard.pdf` with four rendered dashboard screenshots.

If the `.pbix` file is available locally, place it here as `bluestock_mf.pbix` before final GitHub submission. The PDF/screenshots prove the 4-page dashboard design and visuals.
